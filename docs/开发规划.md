# 开发规划

## 阶段目标

1. v0.1.0：工程骨架、测试框架、箱号校验、候选提取。
2. v0.2.0：文件扫描、分类输出、Excel 汇总。
3. v0.3.0：Tesseract OCR 与 PDF 转图。
4. v0.4.0：Tkinter 桌面界面。
5. v1.0.0：PyInstaller Windows 打包交付。

## 当前约束

- 第一版只支持 Windows。
- 第一版保持完全离线。
- 第一版只支持图片和 PDF。
- OCR 引擎先使用 Tesseract，不直接接入 PaddleOCR。
- UI 先使用 Tkinter，不做复杂交互和并行处理。
